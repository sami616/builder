{
  "name": "pager",
  "private": true,
  "scripts": {
    "editor": "pnpm --filter @repo/editor",
    "lib": "pnpm --filter @repo/lib",
    "blocks": "pnpm --filter @repo/blocks"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
